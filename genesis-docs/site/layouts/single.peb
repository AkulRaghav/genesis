{% extends "base.peb" %}
{% block content %}
<article>
    <h1>{{ page.title }}</h1>
    {% if page.description %}
    <p style="color: #6b7280; margin-bottom: 2rem;">{{ page.description }}</p>
    {% endif %}
    <div>{{ content | raw }}</div>
</article>
{% endblock %}
